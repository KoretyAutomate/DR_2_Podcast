# Fish Speech Docker — Environment Variables
# Copy to .env and fill in values:  cp .env.example .env

# HuggingFace token (only needed for gated repositories)
# Get one at: https://huggingface.co/settings/tokens
HF_TOKEN=

# API server port exposed on the host
# 8080 is taken by SearXNG on this machine — Fish Speech uses 8082
API_PORT=8082

# WebUI port
WEBUI_PORT=7860
