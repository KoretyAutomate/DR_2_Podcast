# =============================================================================
# DR_2_Podcast — Pinned Dependencies
# Generated from conda env `podcast_flow` (Python 3.11) on 2026-02-28
# =============================================================================

# --- Core / Pipeline Framework ---
crewai[tools]==1.7.2
crewai-tools==1.7.2
python-dotenv==1.1.1
pydantic==2.11.10
prefect>=3.0,<4

# --- LLM / AI Clients ---
openai==1.83.0
httpx==0.28.1

# --- Web UI ---
fastapi==0.127.1
uvicorn==0.40.0

# --- Artifact contracts ---
# dr2_podcast/schemas/*.schema.json are validated at every stage boundary. Both were already
# present as transitive dependencies (crewai, prefect); pinned here because the pipeline now
# imports them directly and a fail-closed validator must not vanish with a transitive bump.
jsonschema==4.25.1
referencing==0.37.0

# --- Research & Scraping ---
requests==2.32.5
beautifulsoup4==4.13.5
defusedxml>=0.7.1
lxml==6.0.2

# --- Document Generation ---
markdown-it-py==4.0.0
weasyprint==68.1

# --- Audio / TTS ---
kokoro==0.9.4
soundfile==0.13.1
numpy==2.2.6
torch==2.5.1
pydub==0.25.1

# --- Japanese NLP ---
jaconv==0.5.0
misaki[ja]==0.9.4
pyopenjtalk==0.4.1
fugashi==1.5.2
unidic-lite==1.0.8
mojimoji==0.0.13
